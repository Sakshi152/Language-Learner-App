import SwiftUI

extension Color {
    static let primaryColor = Color(red: 0.1, green: 0.5, blue: 0.8) // Blue
    static let secondaryColor = Color(red: 0.9, green: 0.3, blue: 0.2) // Red
    static let backgroundColor = Color(.systemBackground)
    static let cardBackground = Color(.secondarySystemBackground)
}
